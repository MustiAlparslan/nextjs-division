/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // Statik export için
  trailingSlash: true, // URL sonlarına '/' ekler (isteğe bağlı)
};

module.exports = nextConfig;
